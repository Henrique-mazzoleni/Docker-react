sudo: required

language: generic

services:
  - docker

before_install:
  - docker build -t henriquemazzoleni/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true henriquemazzoleni/Docker-react npm run test -- --coverage